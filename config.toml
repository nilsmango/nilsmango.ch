# The URL the site will be built for
base_url = "https://nilsmango.ch"
title = "Nils Mango"
description = "Creating things & eliminating errors."


# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# When set to "true", a feed is automatically generated.
generate_feed = true

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [
   { name = "tags", feed = true },
   { name = "categories", feed = true },
]


[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# List of Papaya navigational menu items. Used to construct the navigation bar. See the README for item schema.
menu_items = [
    { name = "Home", url = "/" },
    { name = "About", url = "/about" },
    { name = "Music", url = "/music" },
    { name = "Articles", url = "/articles" },
    { name = "Projects", url = "/projects" },
    { name = "Tags", url = "/tags" },
]
# Footer Navigation Items
footer_nav = [
    { name = "Privacy", url = "/privacy" },
    { name = "Contact", url = "/contact" },
    { name = "RSS/Atom", url = "/atom.xml" },
    { name = "Twitter", url = "https://twitter.com/nilsmango" },
    { name = "YouTube", url = "https://www.youtube.com/nilsmango" },
    { name = "Instagram", url = "https://www.instagram.com/nilsmango/" },
    { name = "GitHub", url = "https://github.com/nilsmango" },


]
