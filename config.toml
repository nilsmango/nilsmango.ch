# The URL the site will be built for
base_url = "https://nilsmango.ch"
title = "Nils Mango"
description = "Creating things & eliminating errors."


# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# When set to "true", a feed is automatically generated.
generate_feed = true

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [
   { name = "tags", feed = true },
   { name = "categories", feed = true },
]




[markdown]

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

highlight_theme = "base16-ocean-dark"

# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true



[extra]
# List of Papaya navigational menu items. Used to construct the navigation bar. See the README for item schema.
menu_items = [
    #{ name = "Home", url = "/" },
    { name = "About", url = "/about" },
    { name = "Blog", url = "/blog" },
    { name = "Projects", url = "/projects" },
    { name = "Music", url = "/music" },
    { name = "Now", url = "/now" },
]
# Footer Navigation Items
footer_nav = [
    { name = "Privacy", url = "/privacy", newtab = false },
    { name = "Contact", url = "/contact", newtab = false },
    { name = "Feed", url = "/atom.xml", newtab = false },
    { name = "Tags", url = "/tags", newtab = false },
    { name = "X", url = "https://twitter.com/nilsmango", newtab = true },
    { name = "YouTube", url = "https://www.youtube.com/nilsmango", newtab = true },
    { name = "Instagram", url = "https://www.instagram.com/nils_mango", newtab = true },
    { name = "GitHub", url = "https://github.com/nilsmango", newtab = true },
    { name = "Ko-fi", url = "https://ko-fi.com/nilsmango", newtab = true },

]
